info:
  name: Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{DEV_API}}/products/17"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
