info:
  name: Update
  type: http
  seq: 3

http:
  method: PUT
  url: "{{DEV_API}}/products/1"
  body:
    type: json
    data: |-
      {
        "name": "Product 2",
        "description": "This is Product 2 description",
        "price": 22
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
