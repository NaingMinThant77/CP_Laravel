info:
  name: Store
  type: http
  seq: 3

http:
  method: POST
  url: "{{DEV_API}}/categories"
  body:
    type: json
    data: |-
      {
       "name": "Clothing",
        "description": "This is clothing description"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
