info:
  name: "Get All "
  type: http
  seq: 1

http:
  method: GET
  url: "{{DEV_API}}/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
